# Quantbuild E1 v1 — XAUUSD Full-Stack Trading Bot
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
pyarrow>=14.0.1
python-dotenv>=1.0
pydantic>=2.4.0
filelock>=3.20.3

# News layer
feedparser>=6.0
httpx>=0.27

# Data sources
dukascopy-python>=4.0
yfinance>=0.2

# Optional: LLM sentiment
openai>=1.0

# Optional: Oanda broker (only if `broker.provider: oanda` in your YAML)
# oandapyV20>=0.7

# Optional: Telegram alerts
# python-telegram-bot>=20

# Optional: Dashboard
# streamlit>=1.30

# Optional: Scheduling (live loop)
# apscheduler>=3.10

# Equity curve export (README / showcase)
matplotlib>=3.7

# Dev / testing
pytest>=9.0.3
pytest-cov
